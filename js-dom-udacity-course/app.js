const form = document.querySelector('form');
form.addEventListener('submit', function(){
    names = document.getElementById('names');
    const namesInput = names.value;
    
    const div = document.querySelector('div');
    
});